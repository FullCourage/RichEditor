<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="ActionImageView">
        <item name="android:layout_width">40dp</item>
        <item name="android:layout_height">40dp</item>
        <item name="android:background">@drawable/btn_colored_material</item>
        <item name="android:contentDescription">@null</item>
        <item name="android:tint">#515151</item>
    </style>

    <style name="up_and_down_dialog" parent="@android:style/Theme.Dialog">
        <!-- 有无边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 背景透明 -->
        <item name="android:windowBackground">#00000000</item>
        <!-- 模糊 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!--无标题 -->
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="pop_animation" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/translate_pop_in</item>
        <item name="android:windowExitAnimation">@anim/translate_pop_out</item>
    </style>
</resources>